{
  "mcpServers": {
    "zsh-tool": {
      "type": "stdio",
      "command": "${CLAUDE_PLUGIN_ROOT}/scripts/run-mcp.sh",
      "env": {
        "ALAN_DB_PATH": "${CLAUDE_PLUGIN_ROOT}/data/alan.db",
        "NEVERHANG_TIMEOUT_DEFAULT": "120",
        "NEVERHANG_TIMEOUT_MAX": "600"
      }
    }
  }
}
